import {createTheme, Theme} from "@mui/material";

export const LightTheme: Theme = createTheme({
  palette: {
    mode: "light",
    bg: {
      primary: "#000000",
      secondary: "#ffffff",
    },
  },
});
